// Lightweight stubs for Mediapipe types used across the codebase
declare type NormalizedLandmark = {
  x: number;
  y: number;
  z: number;
  visibility?: number;
};
declare type NormalizedLandmarkList = NormalizedLandmark[];
declare type LandmarkStream = any;
